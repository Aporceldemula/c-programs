#include <stdio.h>
#include <stdlib.h>

int main(){
 
 double lado,
         area,
         perimetro;

    system("toilet --gay -fpagga CUADRADO");

    /* Entrada de datos */
      printf("Lado: ");
      scanf(" %l", &lado);

       /* Calculos */
      area = lado * lado;
      perimetro = 4 * lado;

      /* Salida de datos */
      printf( "Area: %l\n", sizeof(double)
     "Perimetro: %l\n", sizeof(double)
      area, perimetro  );

 return EXIT_SUCCESS;
};

